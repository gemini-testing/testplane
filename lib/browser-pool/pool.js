'use strict';

var q = require('q'),
    inherit = require('inherit');

module.exports = inherit({
    getBrowser: function(id) { // eslint-disable-line no-unused-vars
        return q();
    },

    freeBrowser: function(browser) { // eslint-disable-line no-unused-vars
        return q();
    }
});
